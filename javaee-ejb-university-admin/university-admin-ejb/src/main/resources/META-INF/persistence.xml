<?xml version="1.0" encoding="UTF-8"?>
<!-- ========================================== -->
<!-- persistence.xml：JPA 持久化单元配置          -->
<!-- 作用：告诉 JPA 如何连接数据库、              -->
<!--      使用哪个数据源、自动建表策略等          -->
<!-- ========================================== -->
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    
    <!-- ========================================== -->
    <!-- 持久化单元：一个数据库连接配置              -->
    <!-- name="UniversityPU"：在 Java 代码中用这个   -->
    <!--   名字引用 @PersistenceContext(unitName="UniversityPU") -->
    <!-- transaction-type="JTA"：使用容器管理的事务  -->
    <!--   JTA = Java Transaction API                -->
    <!--   EJB 默认使用容器管理的事务（CMT），       -->
    <!--   所以这里必须用 JTA                          -->
    <!-- ========================================== -->
    <persistence-unit name="UniversityPU" transaction-type="JTA">
        
        <!-- JPA 实现提供者：EclipseLink（WebSphere 内置） -->
        <provider>org.eclipse.persistence.jpa.PersistenceProvider</provider>
        
        <!-- ========================================== -->
        <!-- JTA 数据源：通过 JNDI 名字引用              -->
        <!-- 这个数据源由 @DataSourceDefinition 注解     -->
        <!-- 在 StudentServiceBean.java 中定义           -->
        <!-- java:app/env/jdbc/UniversityDS              -->
        <!-- java:app/ 是应用级别的 JNDI 命名空间        -->
        <!-- ========================================== -->
        <jta-data-source>java:app/env/jdbc/UniversityDS</jta-data-source>
        
        <!-- ========================================== -->
        <!-- 声明 JPA 实体类（必须手动列出所有实体）    -->
        <!-- JPA 需要知道哪些类要映射到数据库表         -->
        <!-- ========================================== -->
        <class>com.university.student.entity.Student</class>
        <class>com.university.staff.entity.Staff</class>
        <class>com.university.finance.entity.Tuition</class>
        
        <!-- ========================================== -->
        <!-- 属性配置                                    -->
        <!-- ========================================== -->
        <properties>
            <property name="javax.persistence.schema-generation.database.action"
                      value="drop-and-create"/>
            <property name="eclipselink.logging.level" value="FINE"/>
            <property name="eclipselink.logging.level.sql" value="FINE"/>
            <property name="eclipselink.logging.parameters" value="true"/>
        </properties>
        
    </persistence-unit>
</persistence>
